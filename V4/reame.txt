1、优化v3中控制人物移动的逻辑
2、所有组件的对象保存到List<Prop>中，在移动的时候判断是什么类型，并返回该组件的的对象
    1)人物前一格：判断是否是箱子和墙，有箱子则判断第二个，否则人物直接移动
    2)人物前两格：判断是否是箱子和墙，如果是本次不移动，如果不是人物和箱子向前移动一格
3、Jpanel容器里放需要绘制的的内容,将按钮和图片都放到该容器中，再将Jpanel容器添加到JFame窗口中
4、在添加按钮时必须需清空按钮的默认样式，否则显示不正确，而且要是按钮失去焦点，否则键盘监听将会失效